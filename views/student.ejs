<%- layout('base') %>

<link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700;800;900&family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">

<style>
/* ═══════════════════════════════════════════════
   LIBRIKA STUDENT PORTAL — PRISTINE SUPER ADMIN STYLE UI
   Simple, Clean, Bug-Free & 100% Mobile Responsive
═══════════════════════════════════════════════ */
*, *::before, *::after {
    box-sizing: border-box !important;
}

html, body {
    background: #060d1f !important;
    background-color: #060d1f !important;
    color: #f1f5f9 !important;
    margin: 0 !important;
    padding: 0 !important;
    overflow-x: hidden !important;
    width: 100% !important;
    max-width: 100vw !important;
    min-height: 100vh !important;
    font-family: 'Plus Jakarta Sans', sans-serif !important;
}

:root {
    --sp-purple: #818cf8;
    --sp-purple-mid: #6366f1;
    --sp-purple-light: rgba(99, 102, 241, 0.15);
    --sp-green: #34d399;
    --sp-orange: #fb923c;
    --sp-red: #f87171;
    --sp-bg: #060d1f;
    --sp-card-bg: #0d1630;
    --sp-sidebar-bg: #080f22;
    --sp-border: rgba(99, 102, 241, 0.16);
    --sp-text: #f1f5f9;
    --sp-muted: #94a3b8;
}

/* ── LAYOUT SHELL ── */
.st-shell {
    display: flex !important;
    min-height: 100vh !important;
    background: #060d1f !important;
    width: 100vw !important;
    max-width: 100vw !important;
    position: relative !important;
    overflow-x: hidden !important;
}

.st-main {
    flex: 1 !important;
    display: flex !important;
    flex-direction: column !important;
    min-height: 100vh !important;
    margin-left: 280px !important;
    width: calc(100vw - 280px) !important;
    max-width: 100vw !important;
    overflow-x: hidden !important;
    background: #060d1f !important;
    padding-bottom: 30px !important;
}
@media (max-width: 900px) {
    .st-main { margin-left: 0 !important; width: 100vw !important; max-width: 100vw !important; }
}

/* ── TOPBAR ── */
.st-topbar {
    background: rgba(8, 15, 34, 0.92) !important;
    backdrop-filter: blur(16px) !important;
    -webkit-backdrop-filter: blur(16px) !important;
    border-bottom: 1px solid var(--sp-border) !important;
    padding: 0.85rem 1.5rem !important;
    display: flex !important;
    align-items: center !important;
    justify-content: space-between !important;
    position: sticky !important;
    top: 0 !important;
    z-index: 100 !important;
    gap: 12px !important;
}
.st-topbar-left { display: flex !important; align-items: center !important; gap: 12px !important; }
.st-menu-btn {
    background: rgba(255, 255, 255, 0.05) !important;
    border: 1px solid rgba(255, 255, 255, 0.1) !important;
    color: #f1f5f9 !important;
    width: 38px !important; height: 38px !important; border-radius: 10px !important;
    display: flex !important; align-items: center !important; justify-content: center !important;
    font-size: 1.1rem !important; cursor: pointer !important;
}
@media (min-width: 901px) { .st-menu-btn { display: none !important; } }

.st-brand-title {
    font-family: 'Outfit', sans-serif !important;
    font-size: 1.1rem !important;
    font-weight: 800 !important;
    color: #ffffff !important;
}

.st-topbar-right { display: flex !important; align-items: center !important; gap: 10px !important; }
.st-notif-btn {
    background: rgba(255, 255, 255, 0.05) !important;
    border: 1px solid rgba(255, 255, 255, 0.1) !important;
    color: #f1f5f9 !important;
    width: 38px !important; height: 38px !important; border-radius: 50% !important;
    display: flex !important; align-items: center !important; justify-content: center !important;
    font-size: 1.1rem !important; cursor: pointer !important; position: relative !important;
}
.st-notif-badge {
    position: absolute !important; top: -2px !important; right: -2px !important;
    background: #f87171 !important; color: white !important; font-size: 0.65rem !important;
    font-weight: 800 !important; padding: 2px 6px !important; border-radius: 10px !important;
}

/* ── SIDEBAR DRAWER ── */
.st-sidebar {
    width: 280px !important;
    max-width: 82vw !important;
    background: var(--sp-sidebar-bg) !important;
    border-right: 1px solid var(--sp-border) !important;
    display: flex !important;
    flex-direction: column !important;
    position: fixed !important;
    top: 0 !important; left: 0 !important;
    height: 100vh !important; height: 100dvh !important;
    z-index: 9999 !important;
    transition: transform 0.28s cubic-bezier(0.4, 0, 0.2, 1) !important;
    box-shadow: 10px 0 40px rgba(0, 0, 0, 0.6) !important;
    transform: translateX(-105%) !important;
}
@media (max-width: 900px) {
    .st-sidebar.open { transform: translateX(0) !important; box-shadow: 20px 0 60px rgba(0, 0, 0, 0.8) !important; }
}
@media (min-width: 901px) {
    .st-sidebar { transform: translateX(0) !important; position: sticky !important; }
}

.st-sidebar-header {
    padding: 1.1rem 1rem !important;
    border-bottom: 1px solid var(--sp-border) !important;
    display: flex !important; align-items: center !important; justify-content: space-between !important;
    gap: 10px !important; flex-shrink: 0 !important; background: var(--sp-sidebar-bg) !important;
}
.st-sidebar-user { display: flex !important; align-items: center !important; gap: 10px !important; flex: 1 !important; min-width: 0 !important; }
.st-avatar-lg {
    width: 40px !important; height: 40px !important; border-radius: 50% !important;
    background: linear-gradient(135deg, #6366f1, #a855f7) !important; color: #ffffff !important;
    font-weight: 800 !important; font-size: 1.05rem !important; display: flex !important;
    align-items: center !important; justify-content: center !important; flex-shrink: 0 !important;
    box-shadow: 0 4px 12px rgba(99, 102, 241, 0.3) !important;
}
.st-user-meta { min-width: 0 !important; flex: 1 !important; }
.st-user-name { margin: 0 !important; font-size: 0.9rem !important; font-weight: 800 !important; color: #ffffff !important; white-space: nowrap !important; overflow: hidden !important; text-overflow: ellipsis !important; }
.st-user-school { margin: 1px 0 0 !important; font-size: 0.72rem !important; color: var(--sp-muted) !important; font-weight: 600 !important; white-space: nowrap !important; overflow: hidden !important; text-overflow: ellipsis !important; }

.st-sidebar-close {
    background: rgba(255, 255, 255, 0.06) !important; border: 1px solid rgba(255, 255, 255, 0.12) !important;
    color: var(--sp-muted) !important; width: 32px !important; height: 32px !important; border-radius: 50% !important;
    display: flex !important; align-items: center !important; justify-content: center !important;
    font-size: 0.95rem !important; cursor: pointer !important; flex-shrink: 0 !important;
}

.st-sidebar-nav {
    flex: 1 !important; overflow-y: auto !important; padding: 0.85rem 0.75rem !important;
    display: flex !important; flex-direction: column !important; gap: 4px !important;
}

.st-nav-item {
    display: flex !important; align-items: center !important; height: 48px !important; min-height: 48px !important;
    padding: 0 12px !important; border-radius: 12px !important; color: var(--sp-muted) !important;
    font-size: 0.88rem !important; font-weight: 600 !important; text-decoration: none !important;
    background: transparent !important; border: none !important; border-left: 3.5px solid transparent !important;
    width: 100% !important; cursor: pointer !important; transition: all 0.2s ease !important;
    box-sizing: border-box !important; white-space: nowrap !important;
}
.st-nav-item:hover { background: rgba(99, 102, 241, 0.1) !important; color: #a5b4fc !important; }
.st-nav-item.active { background: rgba(99, 102, 241, 0.18) !important; color: #ffffff !important; border-left: 4px solid #818cf8 !important; font-weight: 700 !important; }

.st-nav-icon { width: 24px !important; height: 24px !important; font-size: 1.15rem !important; display: inline-flex !important; align-items: center !important; justify-content: center !important; flex-shrink: 0 !important; margin-right: 12px !important; }
.st-nav-text { flex: 1 !important; text-align: left !important; white-space: nowrap !important; overflow: hidden !important; text-overflow: ellipsis !important; }

.st-sidebar-footer {
    padding: 0.85rem 0.75rem !important; border-top: 1px solid var(--sp-border) !important;
    flex-shrink: 0 !important; background: var(--sp-sidebar-bg) !important; display: flex !important; flex-direction: column !important; gap: 6px !important;
}
.st-footer-btn {
    display: flex !important; align-items: center !important; height: 44px !important; min-height: 44px !important;
    padding: 0 12px !important; border-radius: 10px !important; font-size: 0.85rem !important; font-weight: 700 !important;
    color: var(--sp-muted) !important; text-decoration: none !important; background: rgba(255, 255, 255, 0.05) !important;
    border: 1px solid rgba(255, 255, 255, 0.08) !important; transition: all 0.2s ease !important; width: 100% !important; cursor: pointer !important;
}
.st-footer-btn:hover { background: rgba(99, 102, 241, 0.15) !important; color: #a5b4fc !important; }
.st-footer-btn.danger { background: rgba(239, 68, 68, 0.12) !important; color: #f87171 !important; border-color: rgba(239, 68, 68, 0.2) !important; }

.st-overlay-dim {
    position: fixed !important; inset: 0 !important; background: rgba(3, 7, 18, 0.8) !important;
    backdrop-filter: blur(8px) !important; -webkit-backdrop-filter: blur(8px) !important;
    z-index: 9998 !important; opacity: 0 !important; pointer-events: none !important; transition: opacity 0.28s ease !important;
}
.st-overlay-dim.active { opacity: 1 !important; pointer-events: auto !important; }

/* ── CONTENT AREA ── */
.st-content { padding: 1.25rem !important; display: flex !important; flex-direction: column !important; gap: 1.25rem !important; background: #060d1f !important; }

/* ── METRIC STAT CARDS GRID ── */
.st-stats-grid { display: grid !important; grid-template-columns: repeat(2, 1fr) !important; gap: 12px !important; width: 100% !important; }
@media (min-width: 901px) { .st-stats-grid { grid-template-columns: repeat(4, 1fr) !important; } }

.st-stat-card {
    background: linear-gradient(145deg, #0d1630, #111c3a) !important;
    border: 1px solid var(--sp-border) !important; border-radius: 18px !important;
    padding: 14px 12px !important; display: flex !important; flex-direction: column !important;
    align-items: center !important; text-align: center !important; justify-content: center !important; min-height: 105px !important;
}
.st-stat-icon { font-size: 1.4rem !important; margin-bottom: 4px !important; }
.st-stat-val { font-family: 'Outfit', sans-serif !important; font-size: 1.4rem !important; font-weight: 900 !important; color: #ffffff !important; line-height: 1.1 !important; }
.st-stat-lbl { font-size: 0.72rem !important; color: var(--sp-muted) !important; font-weight: 700 !important; margin-top: 2px !important; }

/* ── CARDS & TABLES ── */
.st-card {
    background: linear-gradient(145deg, #0d1630, #111c3a) !important;
    border: 1px solid var(--sp-border) !important; border-radius: 20px !important;
    padding: 1.25rem !important; color: #f1f5f9 !important; width: 100% !important; box-sizing: border-box !important; overflow: hidden !important;
}

.st-table-container { width: 100% !important; max-width: 100% !important; overflow-x: auto !important; -webkit-overflow-scrolling: touch !important; margin-top: 10px !important; border-radius: 14px !important; }
table.st-table { width: 100% !important; border-collapse: collapse !important; text-align: left !important; font-size: 0.85rem !important; }
table.st-table th { background: rgba(99, 102, 241, 0.1) !important; color: #a5b4fc !important; font-weight: 800 !important; padding: 12px 14px !important; border-bottom: 1px solid var(--sp-border) !important; white-space: nowrap !important; }
table.st-table td { padding: 12px 14px !important; border-bottom: 1px solid rgba(99, 102, 241, 0.08) !important; color: #94a3b8 !important; vertical-align: middle !important; word-break: break-word !important; }

/* ── BUTTONS ── */
.st-btn {
    display: inline-flex !important; align-items: center !important; justify-content: center !important;
    gap: 8px !important; padding: 9px 16px !important; min-height: 40px !important; border-radius: 12px !important;
    font-size: 0.85rem !important; font-weight: 700 !important; cursor: pointer !important;
    transition: all 0.2s ease !important; text-decoration: none !important; border: none !important;
    background: rgba(99, 102, 241, 0.15) !important; color: #a5b4fc !important; box-sizing: border-box !important;
}
.st-btn-primary { background: linear-gradient(135deg, #6366f1, #a855f7) !important; color: #ffffff !important; box-shadow: 0 4px 14px rgba(99, 102, 241, 0.3) !important; }
.st-btn-danger { background: rgba(239, 68, 68, 0.15) !important; color: #f87171 !important; border: 1px solid rgba(239, 68, 68, 0.25) !important; }
.st-btn-success { background: rgba(16, 185, 129, 0.15) !important; color: #34d399 !important; border: 1px solid rgba(16, 185, 129, 0.25) !important; }
.st-btn-xs { padding: 4px 10px !important; min-height: 32px !important; font-size: 0.75rem !important; border-radius: 8px !important; }

/* REMOVE BOTTOM NAVS */
.bottom-nav, .portal-bottom-nav, nav.bottom-nav, .sp-bottom-nav, .ld-bottom-nav { display: none !important; height: 0 !important; visibility: hidden !important; }
</style>

<div class="st-overlay-dim" id="studentDim" onclick="toggleStudentSidebar()"></div>

<div class="st-shell">
    <!-- ══ SIDEBAR DRAWER ══ -->
    <aside class="st-sidebar" id="studentSidebar">
        <!-- Header -->
        <div class="st-sidebar-header">
            <div class="st-sidebar-user">
                <% const stuName = (typeof session !== 'undefined' && session && (session.user_name || session.name)) ? (session.user_name || session.name) : (typeof user_name !== 'undefined' ? user_name : 'Student'); %>
                <% const stuInitial = stuName.trim()[0].toUpperCase(); %>
                <div class="st-avatar-lg">
                    <%= stuInitial %>
                </div>
                <div class="st-user-meta">
                    <h4 class="st-user-name"><%= stuName %></h4>
                    <p class="st-user-school"><%= (typeof session !== 'undefined' && session && session.school_name) ? session.school_name : 'E-Pathshala Network' %></p>
                </div>
            </div>
            <button class="st-sidebar-close" onclick="toggleStudentSidebar()" aria-label="Close sidebar">✕</button>
        </div>

        <!-- Navigation List -->
        <nav class="st-sidebar-nav">
            <a href="/student" class="st-nav-item active">
                <span class="st-nav-icon">🏠</span>
                <span class="st-nav-text">Dashboard</span>
            </a>
            <a href="/student/browse" class="st-nav-item">
                <span class="st-nav-icon">📚</span>
                <span class="st-nav-text">Digital Library</span>
            </a>
            <a href="/digital-library" class="st-nav-item">
                <span class="st-nav-icon">🌐</span>
                <span class="st-nav-text">Community Library</span>
            </a>
            <a href="/student/my-publications" class="st-nav-item">
                <span class="st-nav-icon">📖</span>
                <span class="st-nav-text">My Books</span>
            </a>
            <a href="/student/assignments" class="st-nav-item">
                <span class="st-nav-icon">📝</span>
                <span class="st-nav-text">Assignments</span>
            </a>
            <a href="/student/requests" class="st-nav-item">
                <span class="st-nav-icon">📋</span>
                <span class="st-nav-text">Book Requests</span>
            </a>
            <a href="/student/calendar" class="st-nav-item">
                <span class="st-nav-icon">📅</span>
                <span class="st-nav-text">Calendar</span>
            </a>
            <a href="/student/goals" class="st-nav-item">
                <span class="st-nav-icon">🎯</span>
                <span class="st-nav-text">Reading Goals</span>
            </a>
            <a href="/student/browse?ai=true" class="st-nav-item">
                <span class="st-nav-icon">🔍</span>
                <span class="st-nav-text">AI Search</span>
            </a>
            <a href="/student/notifications" class="st-nav-item">
                <span class="st-nav-icon">🔔</span>
                <span class="st-nav-text">Notifications</span>
            </a>
            <a href="/student/profile" class="st-nav-item">
                <span class="st-nav-icon">👤</span>
                <span class="st-nav-text">My Profile</span>
            </a>
            <a href="/student/settings" class="st-nav-item">
                <span class="st-nav-icon">⚙️</span>
                <span class="st-nav-text">Settings</span>
            </a>
            <a href="/student/help" class="st-nav-item">
                <span class="st-nav-icon">❓</span>
                <span class="st-nav-text">Help & Support</span>
            </a>
        </nav>

        <!-- Sticky Footer -->
        <div class="st-sidebar-footer">
            <a href="/logout" class="st-footer-btn danger">
                <span class="st-nav-icon">🚪</span>
                <span>Logout</span>
            </a>
        </div>
    </aside>

    <!-- ══ MAIN CONTENT ══ -->
    <main class="st-main">
        <!-- Topbar -->
        <div class="st-topbar">
            <div class="st-topbar-left">
                <button class="st-menu-btn" onclick="toggleStudentSidebar()" aria-label="Menu">☰</button>
                <span class="st-brand-title">🎓 Student Portal</span>
            </div>
            <div class="st-topbar-right">
                <a href="/student/browse" class="st-btn st-btn-primary st-btn-xs">📚 Catalog</a>
                <a href="/student/notifications" class="st-notif-btn">
                    🔔
                    <% const notifBadgeCount = (typeof stats !== 'undefined' && stats) ? ((stats.due_soon_count || 0) + (stats.overdue_count || 0)) : 0; %>
                    <% if (notifBadgeCount > 0) { %><span class="st-notif-badge"><%= notifBadgeCount %></span><% } %>
                </a>
            </div>
        </div>

        <div class="st-content">
            <!-- Hero Welcome Card -->
            <div class="st-card" style="background: linear-gradient(135deg, #1e1b4b, #312e81) !important;">
                <h2 style="font-family: 'Outfit'; font-size: 1.4rem; color: #ffffff; margin: 0 0 6px 0;">Hello, <%= (typeof session !== 'undefined' && session && (session.user_name || session.name)) ? (session.user_name || session.name) : 'Student' %>! 👋</h2>
                <p style="margin: 0; color: #a5b4fc; font-size: 0.88rem;">Welcome back to your Digital Library. Track your reading goals & explore community books!</p>
            </div>

            <!-- Stats Grid -->
            <div class="st-stats-grid">
                <div class="st-stat-card">
                    <div class="st-stat-icon">📚</div>
                    <div class="st-stat-val"><%= (typeof stats !== 'undefined' && stats && stats.borrowed_count) ? stats.borrowed_count : (typeof issued_count !== 'undefined' ? issued_count : 0) %></div>
                    <div class="st-stat-lbl">Active Loans</div>
                </div>
                <div class="st-stat-card">
                    <div class="st-stat-icon">🎯</div>
                    <div class="st-stat-val"><%= (typeof stats !== 'undefined' && stats && stats.goal_pages) ? stats.goal_pages : 20 %></div>
                    <div class="st-stat-lbl">Daily Goal Pages</div>
                </div>
                <div class="st-stat-card">
                    <div class="st-stat-icon">🏆</div>
                    <div class="st-stat-val"><%= (typeof stats !== 'undefined' && stats && stats.reader_score) ? stats.reader_score : 100 %></div>
                    <div class="st-stat-lbl">Reader Score</div>
                </div>
                <div class="st-stat-card">
                    <div class="st-stat-icon">🔥</div>
                    <div class="st-stat-val" style="color: #fb923c;"><%= (typeof stats !== 'undefined' && stats && stats.streak) ? stats.streak : 1 %> Day</div>
                    <div class="st-stat-lbl">Reading Streak</div>
                </div>
            </div>

            <!-- Currently Reading / Issued Books Table -->
            <div class="st-card">
                <h3 style="font-family: 'Outfit'; font-size: 1.1rem; color: #ffffff; margin: 0 0 10px 0;">📖 My Borrowed Books</h3>
                <div class="st-table-container">
                    <table class="st-table">
                        <thead>
                            <tr>
                                <th>Book Title</th>
                                <th>Issue Date</th>
                                <th>Due Date</th>
                                <th>Status</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% if (typeof issued_books !== 'undefined' && issued_books && issued_books.length > 0) { %>
                                <% issued_books.forEach(b => { %>
                                    <tr>
                                        <td style="font-weight: 700; color: #ffffff;"><%= b.title %></td>
                                        <td><%= b.issue_date %></td>
                                        <td><%= b.due_date %></td>
                                        <td>
                                            <% if (b.is_overdue) { %>
                                                <span style="color: #f87171; font-weight: 700;">OVERDUE</span>
                                            <% } else { %>
                                                <span style="color: #34d399; font-weight: 700;">Issued</span>
                                            <% } %>
                                        </td>
                                    </tr>
                                <% }) %>
                            <% } else { %>
                                <tr><td colspan="4" style="text-align: center; color: var(--sp-muted);">No active book loans. <a href="/student/browse" style="color: #818cf8;">Browse Digital Library</a></td></tr>
                            <% } %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </main>
</div>

<script>
function toggleStudentSidebar() {
    const sb = document.getElementById('studentSidebar');
    const dim = document.getElementById('studentDim');
    if (!sb) return;
    sb.classList.toggle('open');
    if (dim) dim.classList.toggle('active', sb.classList.contains('open'));
}

function autoCloseStudentSidebar() {
    if (window.innerWidth <= 900) {
        const sb = document.getElementById('studentSidebar');
        const dim = document.getElementById('studentDim');
        if (sb) sb.classList.remove('open');
        if (dim) dim.classList.remove('active');
    }
}

document.addEventListener('keydown', (e) => {
    if (e.key === 'Escape') autoCloseStudentSidebar();
});
</script>
